{% extends 'base2.html.twig' %}

{% block title %}Edit Project{% endblock %}

{% block body %}
    <div class="container">
        <h2 class="text-center mt-5 mb-3">Edit Categorie</h2>
        <div class="card">
            <div class="card-header">
                <a class="btn btn-outline-info float-right" href="{{ path('app_categorie_index') }}">
                    View All Categories
                </a>
            </div>
            <div class="card-body">
                {% include 'categorie/_form.html.twig' with {'button_label': 'Update categorie', 'button_color': 'btn-outline-success'}  %}
            </div>
        </div>
    </div>
{% endblock %}